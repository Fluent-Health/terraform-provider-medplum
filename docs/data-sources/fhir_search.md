---
page_title: "medplum_fhir_search Data Source - medplum"
subcategory: "FHIR Data"
description: |-
  Counts FHIR resources matching a search (via _summary=count). Useful for previewing migration scope.
---

# medplum_fhir_search (Data Source)

Counts FHIR resources matching a search (via `_summary=count`). Useful for previewing migration scope.

The `search` is a raw FHIR query string. The provider appends `_summary=count` and returns the server's `Bundle.total`. To preview how many resources a `medplum_fhir_data_migration` still has to process, include the same `_tag:not=<marker_system>/<name>|<hash>` filter the migration uses — the count then converges to zero as the migration completes.

## Example Usage

```terraform
# Count how many resources match a FHIR search (via `_summary=count`). Useful
# for previewing migration scope — combine with the migration's own `_tag:not`
# marker to see how many resources remain unmigrated.
data "medplum_fhir_search" "diet_intake" {
  target_resource_type = "QuestionnaireResponse"
  search               = "questionnaire=DietIntake"
}

output "diet_intake_total" {
  value = data.medplum_fhir_search.diet_intake.total
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `search` (String) Raw FHIR search query (e.g. `questionnaire=X&_tag:not=...`).
- `target_resource_type` (String)

### Read-Only

- `total` (Number) Total matching resources (`Bundle.total`). Attribute is named `total`, not `count`, because `count` is a Terraform-reserved name.
