---
page_title: "medplum_project Resource - medplum"
subcategory: "Project Structure"
description: |-
  A Medplum Project. Creation requires super-admin credentials.
---

# medplum_project (Resource)

A Medplum Project. Creation requires super-admin credentials.

Project creation calls Medplum's `Project/$init` operation, which requires super-admin credentials. Configure the provider with `email`/`password` or a super-admin `access_token` when managing projects.

## Example Usage

```terraform
# Create a new Medplum project.
# Project creation calls Project/$init and requires super-admin credentials.
# Configure the provider with email/password or a super-admin access_token.
resource "medplum_project" "patient_portal" {
  name        = "patient-portal"
  description = "Patient-facing portal for appointment scheduling and records access."

  # Optional list of feature flags to enable on the project.
  features = ["smart-app-launch"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)

### Optional

- `description` (String)
- `features` (List of String) Project features. Omit when none.

### Read-Only

- `id` (String) The ID of this resource.


